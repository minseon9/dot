# ?? Encoding ?? ?? ???

**???:** 2025-11-05  
**??:** read_file/write_file ? ??? ??? ?? ??

---

## ?? ??

- `read_file` ?? `write_file` ? ??? `???`? ???
- UTF-8 encoding? ??? ???? ??
- ??? ?? ? ?? ??? ??

---

## ?? ??

### 1. ??? UTF-8 Encoding ??

**?? ?? ?? ? UTF-8? ????? ??:**

```python
# Python ??
with open(file_path, 'r', encoding='utf-8') as f:
    content = f.read()

with open(file_path, 'w', encoding='utf-8') as f:
    f.write(content)
```

```javascript
// Node.js ??
const fs = require('fs');
const content = fs.readFileSync(file_path, 'utf8');
fs.writeFileSync(file_path, content, 'utf8');
```

### 2. ???????? ??

**?? ????? ??? ??? ??:**

```xml
<critical>CRITICAL: When reading/writing files with Korean text, always use UTF-8 encoding and verify Korean characters are preserved correctly. If Korean text appears as ???, re-read the file with explicit UTF-8 encoding or re-save it.</critical>
```

### 3. ?? ?? ??

**?? ?? ? ??? ??:**

```xml
<action>Save file with UTF-8 encoding</action>
<action>Re-read file to verify Korean characters are preserved correctly</action>
<action>If Korean text appears as ???:
  - Re-read file with explicit UTF-8 encoding
  - If still broken, re-save file with UTF-8 encoding
  - Verify again
</action>
```

### 4. Git Commit ???

**Git commit ????? UTF-8 ??:**

```bash
git commit -m "feat: ?? ???" --encoding=utf-8
```

?? git config ??:
```bash
git config --global i18n.commitencoding utf-8
git config --global i18n.logoutputencoding utf-8
```

---

## ?????? ?????

### ?? ??
- [ ] UTF-8 encoding ??
- [ ] ?? ?? ??
- [ ] ?? ?? ? ???

### ?? ??
- [ ] UTF-8 encoding ??
- [ ] ?? ? ???? ??
- [ ] ?? ?? ?? ??

### Git ??
- [ ] Commit ??? UTF-8
- [ ] ?? ?? UTF-8

---

## ?? ??

1. **?? ?????? ?? encoding ?? ??**
2. **?? ?? ? ?? ?? ?? ??**
3. **?? ?? ? ?? ??? ??**

---

## ??

- Python: `open(file, 'r', encoding='utf-8')`
- Node.js: `fs.readFileSync(file, 'utf8')`
- Git: `git config --global i18n.commitencoding utf-8`
